# Consul Service Mesh - Architecture Diagrams

## 1. High-Level Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                            CLIENT LAYER                              │
│                     (Browser, Mobile App, API Client)                │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               │ HTTPS
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                        API GATEWAY SERVICE                           │
│                         (Port 8080)                                  │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  • Rate Limiting (100 req/s)                               │    │
│  │  • Request Caching (L1: Caffeine, L2: Redis)              │    │
│  │  • OAuth2/Keycloak Integration                             │    │
│  │  • Circuit Breaker (Resilience4j)                          │    │
│  │  • Request Coalescing                                      │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              ENVOY SIDECAR PROXY                           │    │
│  │  • mTLS Termination                                        │    │
│  │  • Load Balancing (Least Request)                          │    │
│  │  • Retry Logic (3 retries, 3s timeout)                     │    │
│  │  • Circuit Breaking (5 consecutive errors)                 │    │
│  │  • Metrics Export (Prometheus)                             │    │
│  │  • Distributed Tracing (Jaeger)                            │    │
│  └────────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               │ mTLS Encrypted
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        │                      │                      │
┌───────▼────────┐   ┌─────────▼────────┐   ┌────────▼────────┐
│  Business      │   │  IAM Service     │   │  Future         │
│  Service       │   │  (Port 8081)     │   │  Services       │
│  (Port 8081)   │   │                  │   │                 │
│ ┌────────────┐ │   │ ┌──────────────┐ │   │                 │
│ │ Spring Boot│ │   │ │Spring WebFlux│ │   │                 │
│ │ CQRS/ES    │ │   │ │OAuth2/Casbin │ │   │                 │
│ │ Casbin RBAC│ │   │ │Policy Engine │ │   │                 │
│ └────────────┘ │   │ └──────────────┘ │   │                 │
│                │   │                  │   │                 │
│ ┌────────────┐ │   │ ┌──────────────┐ │   │ ┌─────────────┐ │
│ │   Envoy    │ │   │ │    Envoy     │ │   │ │   Envoy     │ │
│ │  Sidecar   │ │   │ │   Sidecar    │ │   │ │  Sidecar    │ │
│ └────────────┘ │   │ └──────────────┘ │   │ └─────────────┘ │
└────────┬───────┘   └────────┬─────────┘   └─────────────────┘
         │                    │
         │                    │
         └────────────────────┼─────────────────────────┐
                              │                         │
                              │ Service Discovery       │
                              │ & Health Checks         │
                              │                         │
┌─────────────────────────────▼─────────────────────────▼─────────────┐
│                      CONSUL SERVER                                   │
│                    (Service Mesh Control Plane)                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  • Service Registry (Catalog)                              │    │
│  │  • Health Checking (10s interval)                          │    │
│  │  • Configuration Management (KV Store)                     │    │
│  │  • Connect CA (Certificate Authority)                      │    │
│  │  • Service Intentions (Security Policies)                  │    │
│  │  • Service Router/Splitter/Resolver                        │    │
│  └────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         │                    │                    │
┌────────▼──────┐   ┌─────────▼────────┐   ┌──────▼───────────┐
│  PostgreSQL   │   │  Redis Cluster   │   │  MinIO + Keycloak│
│  Master/Slave │   │  6 Nodes         │   │                  │
│  (Port 5432)  │   │  (Port 6379)     │   │                  │
└───────────────┘   └──────────────────┘   └──────────────────┘
```

---

## 2. Service Mesh Data Plane

```
┌─────────────────────────────────────────────────────────────────┐
│                         REQUEST FLOW                            │
└─────────────────────────────────────────────────────────────────┘

1. Client Request
   │
   ▼
┌──────────────────────────────────────┐
│  Gateway Service (Application)       │
│  • Receive HTTP request              │
│  • Apply rate limiting               │
│  • Check L1/L2 cache                 │
│  • Route to backend                  │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Gateway Envoy Sidecar (Outbound)    │
│  ┌────────────────────────────────┐  │
│  │ 1. Resolve service via Consul  │  │
│  │    └─> business-service        │  │
│  │                                 │  │
│  │ 2. Load Balance (Least Request)│  │
│  │    └─> Pick healthiest instance│  │
│  │                                 │  │
│  │ 3. Initiate mTLS handshake     │  │
│  │    └─> Use Consul CA cert      │  │
│  │                                 │  │
│  │ 4. Send encrypted request      │  │
│  │    └─> HTTP/2 over TLS         │  │
│  │                                 │  │
│  │ 5. Apply retry if needed       │  │
│  │    └─> Max 3 retries, 3s each  │  │
│  │                                 │  │
│  │ 6. Record metrics & traces     │  │
│  │    └─> Prometheus + Jaeger     │  │
│  └────────────────────────────────┘  │
└──────────────┬───────────────────────┘
               │
               │ mTLS Encrypted
               │ (TLS 1.3)
               │
               ▼
┌──────────────────────────────────────┐
│  Business Envoy Sidecar (Inbound)    │
│  ┌────────────────────────────────┐  │
│  │ 1. Verify mTLS certificate     │  │
│  │    └─> Check Consul CA         │  │
│  │                                 │  │
│  │ 2. Check service intention     │  │
│  │    └─> Allow gateway→business? │  │
│  │                                 │  │
│  │ 3. Apply L7 permissions        │  │
│  │    └─> Check path/method       │  │
│  │                                 │  │
│  │ 4. Decrypt & forward           │  │
│  │    └─> localhost:8081          │  │
│  │                                 │  │
│  │ 5. Record metrics              │  │
│  │    └─> Connection count, etc   │  │
│  └────────────────────────────────┘  │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Business Service (Application)      │
│  • Process request                   │
│  • Execute business logic            │
│  • Query database                    │
│  • Return response                   │
└──────────────┬───────────────────────┘
               │
               │ (Response flows back through Envoy sidecars)
               │
               ▼
            Client
```

---

## 3. Service Discovery Flow

```
┌─────────────────────────────────────────────────────────────────┐
│               SERVICE REGISTRATION & DISCOVERY                   │
└─────────────────────────────────────────────────────────────────┘

STEP 1: Service Starts
   │
   ▼
┌──────────────────────────────────────┐
│  Business Service Container          │
│  • Spring Boot application starts    │
│  • Spring Cloud Consul kicks in      │
│  • Reads CONSUL_HOST env var         │
└──────────────┬───────────────────────┘
               │
               ▼
STEP 2: Registration
   │
   ▼
┌──────────────────────────────────────┐
│  POST /v1/agent/service/register     │
│  To: consul-server:8500              │
│  Body:                               │
│  {                                   │
│    "name": "business-service",       │
│    "port": 8081,                     │
│    "checks": [{                      │
│      "http": ".../actuator/health",  │
│      "interval": "10s"               │
│    }],                               │
│    "connect": {                      │
│      "sidecar_service": {}           │
│    }                                 │
│  }                                   │
└──────────────┬───────────────────────┘
               │
               ▼
STEP 3: Consul Stores Service
   │
   ▼
┌──────────────────────────────────────┐
│  Consul Server                       │
│  • Adds to service catalog           │
│  • Starts health checking (10s)      │
│  • Generates service certificate     │
│  • Notifies watching clients         │
└──────────────┬───────────────────────┘
               │
               ▼
STEP 4: Gateway Discovers Service
   │
   ▼
┌──────────────────────────────────────┐
│  Gateway Envoy Sidecar               │
│  • Watches Consul for changes        │
│  • Receives service update           │
│  • Updates upstream cluster config   │
│  • Now can route to business-service │
└──────────────────────────────────────┘

CONTINUOUS: Health Checking
   │
   ▼
┌──────────────────────────────────────┐
│  Every 10 seconds:                   │
│  Consul → GET /actuator/health       │
│                                      │
│  If response HTTP 200:               │
│    └─> Mark service as "passing"    │
│                                      │
│  If fails 3 consecutive times:       │
│    └─> Mark as "critical"           │
│                                      │
│  If critical > 30s:                  │
│    └─> Deregister service           │
└──────────────────────────────────────┘
```

---

## 4. Circuit Breaker & Outlier Detection

```
┌─────────────────────────────────────────────────────────────────┐
│                   CIRCUIT BREAKER FLOW                           │
└─────────────────────────────────────────────────────────────────┘

NORMAL STATE: Circuit Closed
   │
   ▼
┌──────────────────────────────────────┐
│  Gateway Envoy → Business Envoy      │
│  • Requests flowing normally         │
│  • Tracking error rate per instance  │
│  • All instances healthy             │
└──────────────┬───────────────────────┘
               │
               │ (Instance starts failing)
               │
               ▼
ERROR DETECTION
   │
   ▼
┌──────────────────────────────────────┐
│  Outlier Detection Algorithm         │
│  ┌────────────────────────────────┐  │
│  │ Consecutive 5xx count: 1       │  │
│  │ Consecutive 5xx count: 2       │  │
│  │ Consecutive 5xx count: 3       │  │
│  │ Consecutive 5xx count: 4       │  │
│  │ Consecutive 5xx count: 5       │  │
│  │ ⚠️  THRESHOLD REACHED!          │  │
│  └────────────────────────────────┘  │
└──────────────┬───────────────────────┘
               │
               ▼
INSTANCE EJECTION
   │
   ▼
┌──────────────────────────────────────┐
│  • Remove instance from load balancer│
│  • Start ejection timer (30s)        │
│  • Route traffic to healthy instances│
│  • Emit alert to Prometheus          │
└──────────────┬───────────────────────┘
               │
               │ (After 30 seconds)
               │
               ▼
RE-EVALUATION
   │
   ▼
┌──────────────────────────────────────┐
│  • Allow instance back in pool       │
│  • Send probe request                │
│  • If successful: fully restore      │
│  • If failed: eject for 60s (2x)     │
└──────────────────────────────────────┘

METRICS EMITTED:
┌──────────────────────────────────────┐
│  envoy_cluster_outlier_detection_    │
│  • ejections_active                  │
│  • ejections_consecutive_5xx         │
│  • ejections_enforced_consecutive_5xx│
│                                      │
│  envoy_cluster_circuit_breakers_     │
│  • default_rq_open                   │
│  • default_rq_pending_open           │
└──────────────────────────────────────┘
```

---

## 5. Traffic Splitting (Canary Deployment)

```
┌─────────────────────────────────────────────────────────────────┐
│                    CANARY DEPLOYMENT FLOW                        │
└─────────────────────────────────────────────────────────────────┘

INITIAL STATE: 100% Stable
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  business-service instances:                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  Stable v1  │  │  Stable v1  │  │  Stable v1  │  │
│  │  (100%)     │  │  (100%)     │  │  (100%)     │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  │
└──────────────────────────────────────────────────────┘

STEP 1: Deploy Canary
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  Deploy new version with metadata:                   │
│  • version: "1.1.0-canary"                           │
│  • subset: "canary"                                  │
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  Stable v1  │  │  Stable v1  │  │  Canary v1.1│  │
│  │  (subset:   │  │  (subset:   │  │  (subset:   │  │
│  │   stable)   │  │   stable)   │  │   canary)   │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  │
└──────────────────────────────────────────────────────┘

STEP 2: Configure Traffic Split (10%)
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  Service Splitter Configuration:                     │
│  {                                                   │
│    "Splits": [                                       │
│      {"Weight": 90, "ServiceSubset": "stable"},      │
│      {"Weight": 10, "ServiceSubset": "canary"}       │
│    ]                                                 │
│  }                                                   │
└──────────────┬───────────────────────────────────────┘
               │
               ▼
TRAFFIC FLOW
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  100 Requests come in:                               │
│                                                      │
│  Gateway Envoy → Service Resolver → Splitter        │
│                                                      │
│  90 requests → Stable subset                         │
│     └─> Round-robin between stable instances        │
│                                                      │
│  10 requests → Canary subset                         │
│     └─> Route to canary instance                    │
└──────────────────────────────────────────────────────┘

STEP 3: Monitor Metrics
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  Compare metrics:                                    │
│                                                      │
│  Stable:                                             │
│    • Error rate: 0.1%                                │
│    • P95 latency: 150ms                              │
│    • Throughput: 900 req/s                           │
│                                                      │
│  Canary:                                             │
│    • Error rate: 0.1%  ✅ Same                       │
│    • P95 latency: 145ms ✅ Better                    │
│    • Throughput: 100 req/s                           │
└──────────────┬───────────────────────────────────────┘
               │
               │ (Metrics look good)
               │
               ▼
STEP 4: Gradually Increase Traffic
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  Phase 1: 10% canary  (monitor 30 min)               │
│  Phase 2: 25% canary  (monitor 30 min)               │
│  Phase 3: 50% canary  (monitor 1 hour)               │
│  Phase 4: 75% canary  (monitor 1 hour)               │
│  Phase 5: 100% canary (full rollout)                 │
└──────────────┬───────────────────────────────────────┘
               │
               ▼
STEP 5: Promote Canary → Stable
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  Update service metadata:                            │
│  • Change canary version → "1.1.0" (stable)          │
│  • Remove old stable instances                       │
│  • Reset splitter to 100% stable                     │
└──────────────────────────────────────────────────────┘

ROLLBACK (if issues detected)
   │
   ▼
┌──────────────────────────────────────────────────────┐
│  If error rate > 5%:                                 │
│  • ./canary-deployment.sh business-service 0         │
│  • Routes 100% traffic back to stable                │
│  • Remove canary instances                           │
└──────────────────────────────────────────────────────┘
```

---

## 6. Observability Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    OBSERVABILITY STACK                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────┐
│  Microservices & Infrastructure      │
│  • Gateway, Business, IAM            │
│  • PostgreSQL, Redis, MinIO          │
│  • Envoy Sidecars                    │
└──────────┬───────────────────────────┘
           │
           │ Emit
           │
    ┌──────┴──────┬──────────┬─────────────┐
    │             │          │             │
    ▼             ▼          ▼             ▼
[Metrics]    [Traces]   [Logs]       [Events]

┌─────────────────────────────────────────────────────────────────┐
│                         COLLECTION LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Prometheus (Metrics)           Jaeger (Traces)                 │
│  ┌────────────────────┐         ┌────────────────────┐          │
│  │ • Scrape interval: │         │ • OTLP receiver    │          │
│  │   15s              │         │ • Jaeger receiver  │          │
│  │ • Retention: 30d   │         │ • Zipkin receiver  │          │
│  │ • Service discovery│         │ • Sampling: 100%   │          │
│  │   via Consul       │         │ • Storage: memory  │          │
│  └────────────────────┘         └────────────────────┘          │
│                                                                  │
│  Targets:                        Collectors:                    │
│  • /actuator/prometheus          • Envoy → Jaeger              │
│  • Envoy /stats/prometheus       • Spring → OTLP               │
│  • Consul /v1/agent/metrics      • Context propagation         │
│  • Postgres exporter             • B3 headers                  │
│  • Redis exporter                                              │
└──────────────┬───────────────────────────────────────┬──────────┘
               │                                       │
               ▼                                       ▼
┌─────────────────────────────┐     ┌────────────────────────────┐
│  Grafana (Visualization)    │     │  Alertmanager              │
│  ┌───────────────────────┐  │     │  ┌──────────────────────┐  │
│  │ Datasources:          │  │     │  │ • Email alerts       │  │
│  │ • Prometheus (metrics)│  │     │  │ • Slack integration  │  │
│  │ • Jaeger (traces)     │  │     │  │ • PagerDuty          │  │
│  │ • Consul (services)   │  │     │  │ • Webhook            │  │
│  └───────────────────────┘  │     │  └──────────────────────┘  │
│                             │     │                            │
│  Dashboards:                │     │  Alert Rules (12):         │
│  • Service Mesh Overview    │     │  • Service Down            │
│  • Envoy Global Stats       │     │  • High Error Rate         │
│  • Spring Boot Metrics      │     │  • High Latency            │
│  • JVM Monitoring           │     │  • Circuit Breaker Open    │
│  • Database Performance     │     │  • Pool Exhaustion         │
└─────────────────────────────┘     └────────────────────────────┘

USER WORKFLOWS:

1. Check Service Health:
   Grafana → Service Mesh Overview Dashboard
   → See all services status, latency, error rates

2. Investigate High Latency:
   Grafana → Identify slow service
   → Click trace ID
   → Jump to Jaeger
   → Analyze span waterfall

3. Debug 5xx Errors:
   Grafana → Filter by error status
   → View error rate graph
   → Check Prometheus for detailed metrics
   → View logs in application

4. Monitor Canary Deployment:
   Grafana → Custom dashboard
   → Compare stable vs canary metrics
   → Decision: promote or rollback
```

---

## 7. Security Architecture (mTLS)

```
┌─────────────────────────────────────────────────────────────────┐
│                      mTLS ARCHITECTURE                           │
└─────────────────────────────────────────────────────────────────┘

CERTIFICATE AUTHORITY
   │
   ▼
┌──────────────────────────────────────┐
│  Consul CA (Certificate Authority)   │
│  • Root certificate (self-signed)    │
│  • Intermediate CA                   │
│  • Auto-rotation: 24h                │
└──────────────┬───────────────────────┘
               │
               │ Issues certificates
               │
    ┌──────────┴──────────┬─────────────────┐
    │                     │                 │
    ▼                     ▼                 ▼
┌─────────┐         ┌──────────┐      ┌──────────┐
│ Gateway │         │ Business │      │   IAM    │
│  Cert   │         │   Cert   │      │   Cert   │
└─────────┘         └──────────┘      └──────────┘

mTLS HANDSHAKE:
┌─────────────────────────────────────────────────────────────────┐
│  Gateway Envoy → Business Envoy                                 │
│                                                                  │
│  1. Client Hello                                                │
│     └─> TLS 1.3, cipher suites                                  │
│                                                                  │
│  2. Server Hello + Certificate                                  │
│     └─> Business Envoy sends its certificate                    │
│                                                                  │
│  3. Client Certificate Request                                  │
│     └─> Server requests client cert (mutual TLS)                │
│                                                                  │
│  4. Client Certificate                                          │
│     └─> Gateway Envoy sends its certificate                     │
│                                                                  │
│  5. Certificate Verification                                    │
│     ├─> Check certificate signature (Consul CA)                 │
│     ├─> Check certificate validity (not expired)                │
│     ├─> Check service identity (SPIFFE URI)                     │
│     └─> Check service intention (allowed?)                      │
│                                                                  │
│  6. Encrypted Channel Established                               │
│     └─> AES-256-GCM, forward secrecy                            │
└─────────────────────────────────────────────────────────────────┘

SERVICE IDENTITY (SPIFFE):
┌─────────────────────────────────────────────────────────────────┐
│  Certificate Subject Alternative Name (SAN):                     │
│                                                                  │
│  spiffe://consul.service.consul/ns/default/dc/dc1/              │
│    svc/business-service                                         │
│                                                                  │
│  Components:                                                     │
│  • Trust domain: consul.service.consul                          │
│  • Namespace: default                                           │
│  • Datacenter: dc1                                              │
│  • Service: business-service                                    │
└─────────────────────────────────────────────────────────────────┘

SERVICE INTENTIONS (Authorization):
┌─────────────────────────────────────────────────────────────────┐
│  Before allowing request:                                        │
│                                                                  │
│  1. Extract source service from client certificate              │
│     └─> gateway-service                                         │
│                                                                  │
│  2. Check Consul intention                                      │
│     Query: Can gateway-service → business-service?              │
│                                                                  │
│  3. Check L7 permissions (if configured)                        │
│     ├─> Path: /api/users (allowed ✅)                           │
│     ├─> Method: GET (allowed ✅)                                │
│     └─> Header: X-Tenant-ID (optional)                          │
│                                                                  │
│  4. Allow or Deny                                               │
│     └─> If allowed: forward request                             │
│     └─> If denied: return 403 Forbidden                         │
└─────────────────────────────────────────────────────────────────┘

CERTIFICATE ROTATION:
┌─────────────────────────────────────────────────────────────────┐
│  Every 24 hours:                                                 │
│                                                                  │
│  1. Consul CA generates new certificate                         │
│  2. Envoy receives new cert via xDS API                         │
│  3. Envoy starts using new cert for new connections             │
│  4. Old certificate still valid for 48h (overlap)               │
│  5. After 48h, old certificate expires                          │
│                                                                  │
│  Zero downtime rotation!                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                   DOCKER COMPOSE DEPLOYMENT                      │
└─────────────────────────────────────────────────────────────────┘

Docker Network: consul-mesh (172.20.0.0/16)
   │
   ├─> consul-server          (172.20.0.10)  [Control Plane]
   │
   ├─> postgres               (172.20.0.x)   [Data Layer]
   ├─> redis                  (172.20.0.x)
   ├─> minio                  (172.20.0.x)
   ├─> keycloak               (172.20.0.x)
   │
   ├─> gateway-service        (172.20.0.x)   [Application Layer]
   ├─> gateway-envoy          (172.20.0.x)   [Sidecar]
   │
   ├─> business-service       (172.20.0.x)
   ├─> business-envoy         (172.20.0.x)   [Sidecar]
   │
   ├─> prometheus             (172.20.0.x)   [Observability]
   ├─> grafana                (172.20.0.x)
   ├─> jaeger                 (172.20.0.x)
   ├─> alertmanager           (172.20.0.x)
   │
   └─> postgres-exporter      (172.20.0.x)   [Exporters]
       redis-exporter         (172.20.0.x)

Volume Mounts:
┌──────────────────────────────────────────────────────────────┐
│  consul-data:        Consul state & KV store                 │
│  postgres-data:      Database storage                        │
│  redis-data:         Cache persistence                       │
│  minio-data:         Object storage                          │
│  prometheus-data:    Metrics storage (30 days)               │
│  grafana-data:       Dashboards & settings                   │
└──────────────────────────────────────────────────────────────┘

Health Check Dependencies:
┌──────────────────────────────────────────────────────────────┐
│  consul-server (ready) ──┐                                   │
│                          ├──> gateway-service                │
│                          ├──> business-service               │
│                          └──> *-envoy sidecars               │
│                                                              │
│  postgres (ready) ───────────> business-service              │
│  redis (ready) ───────────────> gateway-service              │
│                                                              │
│  gateway-service ─────────────> gateway-envoy                │
│  business-service ────────────> business-envoy               │
└──────────────────────────────────────────────────────────────┘

Startup Sequence:
   1. consul-server (30s startup)
   2. postgres, redis, minio, keycloak (parallel)
   3. prometheus, grafana, jaeger (parallel)
   4. gateway-service, business-service (60-90s startup)
   5. gateway-envoy, business-envoy (after services)
   6. register-services.sh (manual step)
```

---

**Generated by:** Claude Sonnet 4.5 🤖
**Version:** 1.0.0
**Last Updated:** 2025-12-28
